

Front End: 

[x] Add social Icons sticky on left side. 
[x] Delete site-top icons. 
[x] Add Footer 
[x] Add font awesome link to snippets folder for common links
[x] Add image to about section
[] transfer HTML into React

Back End: 

[] Create blog model with title, body, image. 
[] Create a tag model with word property. 

/*------------------*/
 [x] Add Font Awesome CDN to Snippters folder.
 
 Font Awesome CDN : 

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

/*-------------------------*/

Samples:

Facebook: Makeup by Tiana
IG: tiana.lino

Simone's Page: 
https://simonesharice.com/contact/

Dulce candy's page: 

https://www.dulcecandy.com/


/* --------------------------------- */

Target attribute - anchor tag: 

  - _blank	Opens the linked document in a new window or tab

 -The rel attribute specifies the relationship between the current document and the linked document. åOnly used if the href attribute is present.

  - nofollow	Links to an unendorsed document, like a paid link.
("nofollow" is used by Google, to specify that the Google search spider should not follow that link)

  - noopener	Requires that any browsing context created by following the hyperlink must not have an opener browsing context

/*----------------------------------------*/

The overflow-x property specifies whether to clip the content, add a scroll bar, or display overflow content of a block-level element, when it overflows at the left and right edges.

The :before and :after inserts some context either before or after the content of an element. 

/*-------------------------------------------*/

[] Add to resources in README Repo

[What is !important?](https://css-tricks.com/when-using-important-is-the-right-choice/)
[Chrome DevTools Docs](https://developers.google.com/web/tools/chrome-devtools/)
[Before and after psuedo-elements](https://css-tricks.com/almanac/selectors/a/after-and-before/)

/*-------------------------------------*/
[] Screen shoot devtools steps. 
[] Mark and identify devtool steps
[] Create Medium article

How do I find the css for hover in the devtools? 

Problem: 
  - The site that I was destructing to better understand the css has a sub-menu class that appears on hover. However, when ever I click on the DOM element in the sub-menu class in the element the css for hover isn't there. I clicked on the inspector arrow and in the Elements HTML the DOM element with that class name and I wasn't able to see the CSS. 

Solution: 
  - In the Chrome devtools go to Elements tab in the DOM find the element with the target class name. On the left are three dots click on it and you showed see force state option and click on the :hover option. Now on the DOM element, there should be a yellow dot where the three dots was in the styles tab you should see the style with the hover selector.

